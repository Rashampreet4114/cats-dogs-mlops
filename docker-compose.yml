services:
  api:
    build: .
    image: ${IMAGE:-cats-dogs-api:local}
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
      - ./models:/app/models:ro
    restart: unless-stopped
