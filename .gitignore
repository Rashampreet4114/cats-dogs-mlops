# venv (named 'mlops' per project requirement)
mlops/
venv/
.venv/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Data (tracked via DVC instead - see data/.gitignore, auto-managed by `dvc add`)

# MLflow local tracking store
mlruns/
mlartifacts/

# Logs
logs/*.log

# OS
.DS_Store
